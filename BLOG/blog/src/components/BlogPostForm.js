import React, { useState } from 'react';
import { View, Text, StyleSheet, TextInput, Button } from 'react-native';

/* Create two props:
    1.) onSubmit function to be used to add blog posts to the back end
    2.) initialValues for your state
*/
const BlogPostForm = () => {

  // 2 instances of state (title, content)

  return (
    // Form that submits the title and content of a blog post
    <View>
      
    </View>
  );
};

// Set initial values here 
BlogPostForm.defaultProps = {};

const styles = StyleSheet.create({});

export default BlogPostForm;
